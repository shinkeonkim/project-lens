# 자격증명 및 로컬 상태 — project-lens 레포에는 절대 커밋되지 않아야 함
.env
.env.*
credentials/
*.sqlite3
*.jsonl

# 비밀값은 아니지만 공개하기엔 애매한 정보(실제 GA4/GTM ID, 배포 도메인, 추적 중인
# 개인/조직 프로젝트 전체 목록) — 로컬 참고용으로만 둔다.
docs/private/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# OS
.DS_Store
